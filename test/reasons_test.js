/**
 * Test case for reasons.
 * Runs with mocha.
 */
'use strict'

const reasons = require('../lib/reasons.js')
const assert = require('assert')
const co = require('co')

describe('reasons', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Reasons', () => co(function * () {

  }))
})

/* global describe, before, after, it */
