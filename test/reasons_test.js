/**
 * Test case for reasons.
 * Runs with mocha.
 */
'use strict'

const reasons = require('../lib/reasons.js')
const assert = require('assert')


describe('reasons', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Reasons', async () => {

  })
})

/* global describe, before, after, it */
