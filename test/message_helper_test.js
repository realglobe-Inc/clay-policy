/**
 * Test case for messageHelper.
 * Runs with mocha.
 */
'use strict'

const messageHelper = require('../lib/helpers/message_helper.js')
const assert = require('assert')

describe('message-helper', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Message helper', async () => {

  })
})

/* global describe, before, after, it */
